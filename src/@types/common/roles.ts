export const enum UserRole {
	admin = 'admin',
	manager = 'manager',
	company = 'company',
	user = 'user',
}

export const UserRoles = [UserRole.admin, UserRole.manager, UserRole.company, UserRole.user]
